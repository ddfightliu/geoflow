"""
Backend package for Geoflow (FastAPI).

This top-level `backend/` folder is a convenience wrapper so developers can
run the web API without navigating into the original `geoflow/web/backend`
package. It intentionally mirrors the FastAPI app implementation and keeps
paths relative to the repository root.
"""

__all__ = ["main"]
